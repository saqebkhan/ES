const MilageCost=(distance, milage, price) =>
    Math.round((distance/milage)*price);

    console.log(`The amount need to fill fuel is Rs.${MilageCost(300, 3,110)}`);

    console.log(Math.ceil(12.55));
    